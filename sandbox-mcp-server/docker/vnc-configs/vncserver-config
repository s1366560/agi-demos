# TigerVNC Server Configuration
# Modern INI format for TigerVNC 1.10+
# NOTE: Only include options that TigerVNC actually supports
#
# Unsupported options that cause errors:
# - encoding (removed, use default)
# - compressionLevel (removed, use default)
# - jpegQuality (removed, use default)
# - sendPrimary/sendClipboard (removed, use default)

# Session type (desktop environment to start)
session=xfce

# Screen geometry (resolution)
geometry=1920x1080

# Color depth (24 = true color)
depth=24

# Security settings for container use
# IMPORTANT: Use lowercase 'none' for TigerVNC 1.10+
securitytypes=none

# Allow connections from any host (required for noVNC websockify)
localhost=no
