# Heartbeat

<!-- The agent reads this file periodically during long-running sessions.
     Use it to provide ongoing instructions, reminders, or course corrections.
     
     If this file is empty or contains only comments, the heartbeat check
     is treated as a no-op (HEARTBEAT_OK) and the agent continues normally.
     
     To give the agent a nudge, write actionable instructions below. -->
