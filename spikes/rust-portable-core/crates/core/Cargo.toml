[package]
name = "memstack-core"
description = "Runtime-agnostic portable core (domain + application) for the cross-platform spike."
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
async-trait.workspace = true
thiserror.workspace = true

# NOTE: intentionally NO tokio / no std::time / no spawning here.
# The core must compile to native, wasm32-unknown-unknown, iOS and Android unchanged.
