{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: lets the main window invoke the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
