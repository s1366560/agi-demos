# 05 · 落地路径与风险

## 1. 绞杀者式增量迁移(语言无关)

**严禁大爆炸重写。** 新旧后端迁移期并行,逐能力绞杀替换。

```mermaid
graph LR
    P0[Phase 0<br/>决策 Spike + 架构文档] --> P1[Phase 1<br/>抽取可移植核心]
    P1 --> P2[Phase 2<br/>平台适配器]
    P2 --> P25[Phase 2.5<br/>扩展性/插件宿主]
    P25 --> P3[Phase 3<br/>AI/LLM 抽象]
    P3 --> P4[Phase 4<br/>同步层 local-first]
    P4 --> P5[Phase 5<br/>逐平台上线]
```

| Phase | 内容 | 状态 |
|---|---|---|
| **0 · 决策 Spike** | 最小核心切片编到 `服务器 + WASM + 移动端`,量化体积/性能/DX 后定语言 | ✅ 完成(Rust 选定,见 [04](04-spike-evidence.md)) |
| **1 · 抽取可移植核心** | domain(已纯净)+ application 纯逻辑迁入核心包;ports 落为跨平台 trait | 🚧 进行中(生产级 Cargo workspace 已落地于 `agi-stack/`:`core`+`plugin-host`+`adapters-{mem,device,wasmtime,http-llm,postgres,neo4j}`+`bindings-{wasm,uniffi}`+`apps/{server,gateway,desktop,bench}`,`cargo test --workspace` **480 绿**、服务器全端点 curl 验证;完整 86K LOC Python→Rust 绞杀迁移为多季度工程,标注 future) |
| **2 · 平台适配器** | 每个端口两套实现(server 重型栈 / device 嵌入式栈) | 🎯→✅ 大部分已落地(`adapters-mem` 内存栈、`adapters-device` SQLite+HNSW、`adapters-wasmtime` 沙箱宿主、`adapters-http-llm` 云 LLM、`bindings-wasm` 浏览器、`bindings-uniffi` 移动双端均已编译+测试,见 [04 #11–#20](04-spike-evidence.md);生产 Postgres+pgvector 重型栈标注 future) |
| **2.5 · 扩展性/插件宿主** | 落 `ToolHost`/`PluginRuntime` 端口 + WIT 工具契约;内置工具走 `dyn Trait`;第三方/MCP 走 WASM 沙箱;Skill 落为数据 + Rhai;**热插拔生命周期**(ArcSwap 原子换表 + CP/DP 配置推送 + proxy-wasm 风格 ABI,见 [06 §2](06-agent-core-design.md));**多层插件运行时**(typed 能力注册 + 插件形态分类 + 可插拔 Harness,见 [07](07-plugin-runtime-architecture.md));**控制面→数据面 reconcile**(xDS 风格 version/nonce + ACK/NACK + last-good,见 [08](08-control-data-plane-separation.md))。**绞杀点:现有 30+ 工具与 MCP 沙箱在此分批迁移到统一插件契约** | 🎯→✅ 大部分已落地(热插拔 + CP/DP reconcile + Wasmtime 沙箱宿主 + **L2 Rhai Skill 引擎**均已验证,见 [02](02-extensibility.md)、[04 #9/#10/#18/#23](04-spike-evidence.md);MCP-over-WASM、30+ 工具实际绞杀迁移标注 future) |
| **3 · AI/LLM 抽象** | LLM、embedding、向量检索、重排的 cloud↔on-device 双适配 | 🎯→✅ 部分(**HTTP LLM/Embedding 适配器**(OpenAI/LiteLLM 兼容)+ **cloud↔local DI 切换**已落地、mock-server 测试绿,见 [04 #20](04-spike-evidence.md);**chat 半对活 coding-plan 实测**(GLM `glm-4.6` + MiniMax `MiniMax-M2`,`complete`+`extract_memory` 端到端、推理模型 `<think>` 剥离,见 [04 #39](04-spike-evidence.md));**embedding 半对活 Ollama `/v1/embeddings` 实测**(真 1024 维向量 + 语义合理性,见 [04 #38](04-spike-evidence.md));**新增 `RerankPort` + `HttpRerank` 对活 `BAAI/bge-reranker-base` 实测**(混合检索精排,见 [04 #40](04-spike-evidence.md));**端上向量检索**(HNSW)已落地,见 [04 #19](04-spike-evidence.md);**WS bridge foundation + gateway flip** 已落地,见 [04 #44](04-spike-evidence.md);端上本地 LLM(llama.cpp/Candle)+ **流式 chat token + 完整 WS ConnectionManager**标注 future) |
| **4 · 同步层** | local-first 同步、冲突解决、离线优先;**= 数据面断连自治**(最终一致 + 重连全量重同步,衔接 [08 §7](08-control-data-plane-separation.md) CP/DP 分离) | 🎯→✅ 部分(**version-vector delta + LWW-register `reconcile`** 已落地、离线分叉→重连→最终一致跑通、CRDT 交换/幂等律证伪,见 [04 #21](04-spike-evidence.md);CRDT 富类型(RGA/计数器)与真实持久化传输标注 future) |
| **5 · 逐平台上线** | 先服务器对齐现有功能(绞杀替换旧 Python),再 PC → 移动端 → web-WASM | 🚧 进行中(**构建矩阵已落地** + **生产绞杀迁移已启动**):server/web/桌面/Android/iOS 出厂矩阵已落地([04 #24](04-spike-evidence.md));P1 记忆/情节/召回、P2 登录/租户/项目/成员/邀请/device-code/shares/trust、P4 GraphStore REST/community/enhanced-search/rebuild、P5 sandbox/MCP/desktop/terminal/preview/skill-store、P6 workspace/chat messages/agent mention admission/runtime binding/terminal projection/ready writer/agent-chain admission/tasks/topology/blackboard/plan/outbox worker/pipeline run admission/sandbox-native no-service stage execution/accepted worktree skipped/already-merged/real merge/dirty-main integration + stale dirty-main reattempt + dirty-main repair dependency dispatch + worker_launch conversation binding/Redis cooldown-running/heartbeat marker refresh/orphaned stream stop decision/idle progress watchdog/terminal report fallback/stream reducer foundation/terminal outcome persistence/stream event replay/stream idle progress/stream poll continuation/orphan stop persistence + worker-report supervisor retry admission + worker-report retry context propagation + retry state reset + retry exhaustion + accept-review attempt acceptance + accepted supervisor judge reconciliation + supervisor retry_same_node decision reconciliation + supervisor dispose_node disposition projection + supervisor mark_blocked_human reconciliation + supervisor replan_node recovery request foundation + supervisor create_repair_node repair-node foundation + supervisor request_pipeline durable pipeline request foundation + supervisor wait_pipeline pending gate foundation + supervisor noop audit preservation + attempt worktree checkpoint/setup foundation/Drone source_publish admission failure/git publish + remote branch merge/retry parity + provider-unavailable result + Drone HTTP trigger/poll + deploy params/trusted repo/docker deploy validation + .drone.yml preflight foundation + Drone CLI fallback 均已按共享 schema strangler 方式落地([04 #25](04-spike-evidence.md),[#54](04-spike-evidence.md)-[#57](04-spike-evidence.md),[#58](04-spike-evidence.md)-[#84](04-spike-evidence.md),[#85](04-spike-evidence.md)-[#156](04-spike-evidence.md));C2 正在把巨型 workspace outbox worker 按 feature 拆分,已抽出 agent_mention、worker_stream_watchdog 与测试模块,但父文件仍 >800 行;当前 `cargo test --workspace` **480 绿**。channels + skills filesystem/system/import/export/evolution/tenant config、P6 remaining full supervisor orchestration + full worker runtime execution/crash-retry ownership/full mention runtime/autonomy 仍 future;P2–P8 余下 ~580 端点为多季度工程,逐波次标注 future。 |

> **核心引擎质量(健壮 · 可编排)横跨多阶段**:会话 checkpoint/崩溃恢复(健壮)落在 Phase 1 核心 + 服务器/端上 runner;Plan append-only DAG 编排、HITL suspend/resume、retry/memoization(可编排)落在核心编排层。机制来源与综合设计见 [06-agent-core-design](06-agent-core-design.md),决策见 [ADR-0004](../adr/0004-plan-as-append-only-dag.md)/[0005](../adr/0005-round-boundary-checkpoint.md)/[0006](../adr/0006-hot-plug-via-arcswap-and-proxy-wasm-abi.md)。
>
> **控制流/数据流分离(第三系统轴)亦横跨多阶段**:配置 reconcile 协议(控制面=SSOT、数据面 level-triggered 收敛)落在 Phase 2.5;数据面断连自治(最终一致 + 重连重同步)落在 Phase 4 同步层。综合设计见 [08-control-data-plane-separation](08-control-data-plane-separation.md),决策见 [ADR-0009](../adr/0009-control-data-plane-separation.md)/[0010](../adr/0010-xds-style-config-distribution.md)。

## 2. 必须证伪的高风险项(按优先级)

| # | 风险 | 状态 |
|---|---|---|
| 1 | **运行时无关 async**:core 不绑 tokio 却能在 native/WASM/UniFFI 三处都跑 | ✅ 证伪通过 |
| 2 | **WASM + 本地存储/向量**:wa-sqlite/sqlite-vec 是否可用?否则降级 IndexedDB + 内存 hnsw | ✅ 部分证伪(**生产 `bindings-wasm` 内存路径已通** —— `wasm-bindgen` + `WasmClock` + 内存仓储/向量,node 冒烟 round-trip 绿,见 [04 #16](04-spike-evidence.md);wa-sqlite/IndexedDB 持久层标注 future) |
| 3 | **端上向量检索**:sqlite-vec / usearch / hnsw-rs 在 iOS/Android 交叉编译 | ✅ 通过(纯 Rust **HNSW** `instant-distance` 替 `sqlite-vec` C 扩展以保跨端可移植;`adapters-device` N=10k P50 **2.43 ms**、31× 加速,见 [04 #19](04-spike-evidence.md)) |
| 4 | **UniFFI 复杂类型 + async + 回调**:`Memory`(嵌套 list)能否干净导出?宿主端口能否回注? | ✅ 双端设备产物(`crates/bindings-uniffi`:Android 经 NDK 产真实 `aarch64-linux-android` `.so` + Kotlin 包,见 [04 #12](04-spike-evidence.md);iOS 经 full Xcode 产 XCFramework + Swift 包并在 iPhone 17 模拟器实跑,见 [04 #13](04-spike-evidence.md)) |
| 5 | **图依赖**:端上无 Neo4j,entities/relationships 用 SQLite 关系表或内存 petgraph 近似 | ✅ 部分证伪(**端上图存储 + 遍历 + 排序数学 + 生产 Neo4j 驱动均已落地** —— `GraphStore` 端口 + `GraphEntity`/`Relationship`/`Subgraph` 模型 + 纯 Rust 排序数学 [`crates/core/src/graph.rs`](../../crates/core/src/graph.rs)(RRF/时间衰减/MMR/Jaccard **逐字复刻 Python** `src/infrastructure/graph/`)+ **三套适配器** `InMemoryGraphStore`(`petgraph`,同编 `wasm32`)/`SqliteGraphStore`(SQLite 关系表 + `petgraph`,端上耐久)/**`Neo4jGraphStore`(`neo4rs` Bolt,生产服务器层,对 Python 同一套 Neo4j)**;跨层 parity 测证内存 vs SQLite([04 #31](04-spike-evidence.md))**及内存 vs 活 Docker Neo4j**([04 #32](04-spike-evidence.md))子图/邻居/搜索结果一致;**LLM 驱动 reflexion 抽取(需 `LlmPort`)、Louvain 社区、Episodic/Community 节点**留 future,A 方案);不阻断核心四端可移植 |
| 6 | **端上 LLM**(可选):llama.cpp/Candle 移动端体积与可行性 | future(A 方案):端上本地推理(数百 MB 模型)留后续;云端 LLM 经 [04 #20](04-spike-evidence.md) HTTP 适配器已通,`LlmPort` 抽象使端上实现可后插 |
| 7 | **可移植插件宿主**:核心能否在不破坏"运行时无关+四端可移植"前提下宿主沙箱工具?WASM-in-WASM(浏览器,Wasmi)成立? | ✅ 证伪通过(见 [04 #8](04-spike-evidence.md)) |
| 8 | **会话 checkpoint 崩溃恢复**:轮次边界快照能否在不重复已完成工具的前提下恢复长会话?端上(无 tokio,WASM 无 FS)增量持久化是否成立? | ✅ 证伪通过(`ReActEngine` 轮次边界增量 + 边界 checkpoint;`adapters-mem/tests/agent_recovery.rs` 证内存路径杀进程→恢复**不重调已完成工具**[`CountingToolHost` 计数为 0],`adapters-device/tests/device.rs` 证 SQLite 耐久路径恢复;见 [04 #11](04-spike-evidence.md)、[06 §4](06-agent-core-design.md)、[ADR-0005](../adr/0005-round-boundary-checkpoint.md)) |
| 9 | **热换工具零中断**:ArcSwap 换表 + CP/DP 推送能否在不打断在途会话的前提下启用/禁用/升级工具? | ✅ 部分证伪(换表机制已通,CP/DP 网络下发待办)——`hotplug-demo` 证 v1→v2 热换 + 飞行轮次见旧版本 + enable/disable + shape 分类(见 [04 #9](04-spike-evidence.md)、[07](07-plugin-runtime-architecture.md)、[ADR-0006](../adr/0006-hot-plug-via-arcswap-and-proxy-wasm-abi.md)/[0007](../adr/0007-capability-registration-plugin-model.md)) |
| 11 | **可插拔 Harness**:Agent 执行循环能否做成可第三方替换的 `RuntimeHarness`(embedded vs CLI-backend),`auto` 选择 + 回退,且 runtime 按 `(provider,model)` scoped? | ✅ **完整闭环**——`RuntimeHarness` trait + `HarnessRegistry::select`(`auto` 优先级 + policy pin + 回退),**两个真实 runtime 家族**(embedded 进程内 `EmbeddedHarness` vs CLI 子进程 `CliBackendHarness`,见 [`crates/adapters-cli-harness`](../../crates/adapters-cli-harness),server-only);`PreparedAttempt` 的 `RuntimePlan` 富契约(`normalize_tools`/`classify_outcome`/`is_silent`)已落实为 core 纯数据;core 9 + `adapters-mem` 2 + cli-harness 5 测试(见 [04 #14/#27](04-spike-evidence.md)、[07 §4](07-plugin-runtime-architecture.md)、[ADR-0008](../adr/0008-agent-runtime-as-pluggable-harness.md)) |
| 10 | **Plan 动态 DAG + HITL**:append-only DAG 的 reconcile 幂等、suspend/resume(四类 HITL)在重型(Kameo)与轻量(`MiniOrchestrator`)两版是否一致? | ✅ **完整闭环**——HITL `RequestHuman`+`resume()` 轮次边界挂起/恢复(内存 + SQLite 两路径,不重调已完成工具)**加** `MiniOrchestrator` 驱动 append-only Plan DAG **多节点 reconcile**(钻石 DAG 收敛、mid-reconcile 崩溃恢复复用 Done 步、失败短路保留 last-good、幂等重放,core 4 测试,见 [04 #15/#26](04-spike-evidence.md)、[ADR-0004](../adr/0004-plan-as-append-only-dag.md)/[0005](../adr/0005-round-boundary-checkpoint.md));轻量版 `MiniOrchestrator` 顺序派发已证,Kameo 并行监督 parity 标注 future |
| 12 | **控制流/数据流分离**:控制面发期望态(SSOT)、数据面 level-triggered 自算 diff 收敛;坏配置 NACK 保留 last-good 不致瘫;同版本幂等、陈旧拒绝;断连重连全量重同步 —— 纯同步 reconciler 端上(无 tokio)是否成立? | ✅ 证伪通过(`cp-dp-demo` + `control_data_plane` 6 测试;见 [04 #10](04-spike-evidence.md)、[08](08-control-data-plane-separation.md)、[ADR-0009](../adr/0009-control-data-plane-separation.md)/[0010](../adr/0010-xds-style-config-distribution.md)) |

## 3. 量化指标与 go/no-go 阈值(评分卡实测)

> 由 [`apps/bench`](../../apps/bench)(bin `scorecard`,`cargo run -p agistack-bench --release`)实测产出:延迟在内存核心栈采集、体积最佳努力实读产物;14 行风险全 ✅ → **自动判定并打印「建议:GO」、退出码 0**(`fail==0`)。详见 [04 §1 #22](04-spike-evidence.md)。

| 指标 | 采集方式 | 建议 go 阈值 | 当前(评分卡实测) |
|---|---|---|---|
| WASM 体积(raw / gzip) | bench 实读 pkg / `wasm-opt -Oz` | ≤ 2.5 MB(切片) | ✅ **124.8 KB raw / 60 KB gzip**(生产 `bindings-wasm`,含向量+语义,见 [04 #16](04-spike-evidence.md)/#22) |
| 原生 server release 二进制 | bench 实读 | 对比 Python 数十–数百 MB | ✅ **5.46 MB**(装齐 reqwest+rustls/wasmtime/rusqlite/tokio 后仍数量级小于 Python 运行时,见 [04 #22](04-spike-evidence.md)) |
| iOS lib / Android .so | 链接产物 | ≤ 8 MB/arch(不含本地模型) | ✅ Android 1.5 MB(aarch64 release,见 [04 #12](04-spike-evidence.md));iOS XCFramework 已构建 + 模拟器实跑,链接后体积待 app 实测(见 [04 #13](04-spike-evidence.md)) |
| 单步提取延迟(剔 LLM 网络) | 核心打点(bench ×2000) | ≤ 50 ms | ✅ **P50 0.001 ms · P99 0.003 ms** |
| 关键词 search 延迟 | bench ×2000 | 低延迟 | ✅ **P50 0.69 ms · P99 0.79 ms** |
| 语义 search 延迟(embed+向量) | bench ×2000 | 低延迟 | ✅ **P50 0.30 ms · P99 0.35 ms** |
| 端上向量检索(N=10k, dim=768) | sqlite-vec/hnsw 计时 | P50 ≤ 20 ms | ✅ **设备 HNSW 2.43 ms**(权威,dim=256;暴力 75 ms,31× 加速,recall@10 89.5%,见 [04 #19](04-spike-evidence.md));评分卡内存暴力基线 ~4.6 ms |
| 移动端冷启动首调 | app 内打点 | ≤ 300 ms | ⏳ iPhone 17 模拟器已跑通,精确计时待 app 内打点 |
| 跨 FFI 复杂类型往返 | 微基准 | 无频繁拷贝瓶颈 | ⏳ UniFFI 双端往返已通(#12/#13),专项微基准待补 |
| 每端口平台胶水 LOC | 统计 | 低且可复制 | ✅ ≈ 一个文件 |
| async 跨 UniFFI/WASM | 是否需 hack | 原生支持、不阻塞主线程 | ✅ 原生支持 |
| 会话崩溃恢复正确性 | 杀进程→恢复→比对 | 不丢轮次、不重复已完成工具 | ✅ 通过(内存 + SQLite 两路径,见 [04 #11](04-spike-evidence.md)) |
| 热换工具中断 | 在途会话期间换表计时 | 在途轮次零中断,新轮次毫秒级见新表 | ✅ 部分(demo 证飞行隔离:新调用得 v2、持旧快照仍得 v1;在途轮次中断的会话级量化待 Agent 切片) |
| HITL 暂停→恢复往返 | suspend→外部信号→resume | 四类 HITL 均可暂停/恢复,状态不丢 | ✅ 单类已测(`agent_hitl` + device SQLite 往返;[04 #15](04-spike-evidence.md))·四类齐全待续 |
| CP→DP 配置收敛/坏配置隔离 | reconcile 测试 + `cp-dp-demo` | 坏配置 NACK 不改 last-good;同版本零 churn(`Arc::ptr_eq`);漏推/乱序自愈 | ✅ 证伪通过(6 测试) |
| **go/no-go 总评分卡** | `apps/bench` 汇总 14 风险 | `fail==0` → 退出码 0 + 打印 GO | ✅ **GO**(14/14 ✅,见 [04 #22](04-spike-evidence.md)) |

## 4. 近期待办(Spike 收尾 → Phase 1)

1. **Wasmtime 宿主 + WIT 契约**:✅ **已落地** —— `crates/adapters-wasmtime` 为服务器/桌面提供 Wasmtime 后端的不可信工具宿主:`WasmtimeTool` 实 `plugin_host::Tool`(`Trust::SandboxedWasm`),每调用新 `Store` + **fuel(确定性逐指令预算)+ epoch(墙钟中断)双配额**;`from_bytes` **运行时加载 wasm 字节**(替代 PoC 的构造期裸 `.wat`);工具契约升格为 typed [`wit/tool.wit`](../../crates/adapters-wasmtime/wit/tool.wit)(`tool` world / `scorer` 接口),可运行样例经等价 core-module ABI 实现(完整 Component Model guest 需 `cargo-component`,按评分卡 A 方案标注 future)。`WasmtimeToolFactory` 实 `ToolFactory` 经 manifest `wat` 建沙箱工具,接 `PluginHost::enable`/CP-DP reconcile。**5 单测绿**(加载执行、`ArcSwap` 热换 + 飞行隔离、fuel 耗尽 trap、epoch deadline 确定性 trap、factory 全生命周期);`apps/server` 启动注册沙箱 `score` 工具,经 `POST /v1/tools/call` 端到端跑通。Wasmtime 为 **native-only**,iOS(无 JIT)/浏览器留 Wasmi 兜底;根 `cargo test --workspace` 升至 **42 绿**、core `wasm32` 仍绿(见 [04 #18](04-spike-evidence.md)、[ADR-0002](../adr/0002-untrusted-plugins-wasm-only.md)/[0003](../adr/0003-plugin-host-as-hexagonal-port.md))。后续接 `cargo-component` 完整 Component Model guest CI 固化。
2. **移动端设备产物**:**双端均已产出** —— Android 经 NDK r30 交叉编译 `aarch64-linux-android`,release **1.5 MB**(stripped,含 SQLite C),`file` 验 ELF ARM aarch64 + Kotlin 包(见 [04 #12](04-spike-evidence.md));iOS 经 full Xcode 交叉编译 `aarch64-apple-ios`(+ `-sim`)、组装 **XCFramework** + Swift 包,并在 **iPhone 17 模拟器实跑**冒烟(摄取/关键词/语义检索全绿,见 [04 #13](04-spike-evidence.md),一键 `scripts/build-ios.sh`);后续接 **真机签名分发** + `cargo-ndk`/CI 固化构建,并补端上向量检索与冷启动量化。
3. **端上真向量检索(HNSW)**:✅ **已落地** —— 设备向量索引从暴力 SQLite 余弦扫描升级为纯 Rust **HNSW 近似最近邻**(`crates/adapters-device/src/hnsw.rs`,`instant-distance`),玩具按词 hash-embedding 升为高维**字符 n-gram 哈希**(`adapters-mem::NgramHashEmbedding`)。**刻意不选 `sqlite-vec`**:其 C 扩展 `dlopen` 跨 iOS/Android 正是可移植核心要规避的脆弱性,纯 Rust HNSW 跨所有设备目标零改动(`sqlite-vec` 留作 server 侧同端口可选)。HNSW 即时图不可变,以「写后惰性重建」满足可变 `VectorIndexPort`。`examples/vector_bench.rs` 实测 N=10k dim=256:暴力 P50 **75.2 ms** vs HNSW P50 **2.43 ms**(**31× 加速**,recall@10 **89.5%** 于对抗性随机向量),稳达 ≤20 ms 阈值;跨适配器 parity 测证 HNSW top-1 == 暴力 top-1。根 `cargo test --workspace` 升至 **47 绿**、core `wasm32` 仍绿(`instant-distance`/`rusqlite` 仅入 `adapters-device`)。后续:更高维(768)+ 真 embedding 模型 + 增量插入 HNSW(替一次性重建);`sqlite-vec` server 侧可选。见 [04 #19](04-spike-evidence.md)、[03 §1](03-platform-adapters.md)。
4. **Tauri 桌面**:✅ **已落地** —— `apps/desktop/src-tauri`(`agistack-desktop`)把生产核心 + SQLite **设备**适配器链入 Tauri 2 桌面外壳,`ingest`/`search`/`semantic_search` 暴露为 Tauri 命令、极简 HTML 前端经 `window.__TAURI__.core.invoke` 调用;命令逻辑抽到可**无头**单测的 `DesktopCore`(`futures::executor::block_on` 驱动、零 tokio)。`cargo build` 全树编译链接通过(tauri 2.11 + 系统 WebKit),`cargo test` 1 绿无头 round-trip;crate 故意**排除出 workspace** 以免重依赖拖慢 `cargo test --workspace`(仍 37 绿)(见 [04 #17](04-spike-evidence.md)、[03 §5](03-platform-adapters.md))。后续接 `tauri-cli` 完整 bundle 打包 + OS app-data 路径解析。
5. **完整 go/no-go 评分卡**:✅ **已落地** —— `apps/bench`(bin `scorecard`)在内存核心栈实测 ingest/keyword-search/semantic-search P50/P99(各 ×2000)+ N=10k dim=256 暴力向量 P50/P99,并最佳努力实读 server release 二进制(**5.46 MB**)与 wasm pkg(**124.8 KB raw**)体积,联合前 21 项已验证产物组成 **14 行风险评分表**,`fail==0` 即打印「建议:**GO**」并退出码 0。补齐 §3 大部分未测项(冷启动/FFI 微基准仍 ⏳)。复现:`cargo run -p agistack-bench --release`(见 [04 #22](04-spike-evidence.md))。后续:接 CI 把评分卡作为合并门禁。
8. **Web 包(production wasm-bindgen 绑定)**:✅ **已落地** —— `crates/bindings-wasm` 把生产核心编入浏览器(core-as-guest),`AgistackCore` 暴露 `ingest`/`search`/`semanticSearch`,wasm 侧用真实 `WasmClock`(`Date.now()`);`wasm-pack --target nodejs` + `smoke.cjs` node 冒烟绿,**60 KB gzip**(见 [04 #16](04-spike-evidence.md))。后续接 wa-sqlite/IndexedDB 持久层 + 浏览器内冒烟。
6. **Agent 核心健壮/编排切片**:✅ **健壮 + 编排三轴收口** —— (a) **HITL**:`AgentAction::RequestHuman` + `resume()`,轮次边界 checkpoint(状态即真相)、崩溃恢复重放、单个 HITL suspend/resume,内存 + SQLite 两路径测试绿且不重调已完成工具(见 [04 #15](04-spike-evidence.md));server `POST /v1/agent/resume`。(b) **多节点 Plan DAG 编排**:`crates/core/src/agent/orchestrator.rs` 的 `MiniOrchestrator`(单线程、运行时无关、零 tokio/std::time)驱动 append-only Plan DAG 收敛到底 —— level-triggered `ready()` 幂等重放、mid-reconcile 崩溃恢复复用 Done 步(不重派)、失败短路保留 last-good、重复 reconcile 幂等 no-op,core 4 测试(见 [04 #26](04-spike-evidence.md)、[06 §5](06-agent-core-design.md));Kameo 并行监督树标注 future(parity 点:同一份 reconcile 逻辑即端上轻量核)。(c) **健壮性硬化**:`crates/core/src/agent/doom_loop.rs` 的 `DoomLoopDetector`(滑窗内重复 `(tool,input)` 检测)+ `CostTracker`(rounds/tool-calls/tokens 预算算术)为**确定性触发器**,只点火 trigger;主观裁决经注入 `SupervisorPort` 由 agent 得结构化 `{health,next,rationale}` 再确定性行动 —— **仓库顶层 Agent First 铁律的字面落地**(结构 trigger 确定,语义 verdict 归 agent),`adapters-mem/tests/agent_supervisor.rs` 6 测试绿(见 [04 #28](04-spike-evidence.md)、[ADR-0011](../adr/0011-agent-supervised-verdict.md)、[06 §4](06-agent-core-design.md));supervisor 接真 LLM 判定标注 future(本波用注入端口 + fake 证伪机制)。**注**:热插拔(ArcSwap 工具换表零中断 + 扩展 enable/disable + shape 分类)已由 `crates/plugin-host` + `hotplug-demo` 单独证伪(见 [04 #9](04-spike-evidence.md))。
7. **可插拔 Harness 切片**:✅ **两个真实 runtime 家族已落地** —— 把内置 ReAct 循环抽为 `RuntimeHarness` trait + `HarnessRegistry::select`(`auto` 优先级 + policy pin + 回退,runtime 每轮重解析),内置 `EmbeddedHarness` 包 `ReActEngine`;**第二个非 embedded harness** [`crates/adapters-cli-harness`](../../crates/adapters-cli-harness) 的 `CliBackendHarness`(经 `std::process::Command` spawn 子进程、PreparedAttempt 以 JSON 喂 stdin、TurnOutcome 从 stdout 解析、`owns_native_compaction` 标志,**server-only**)证 `HarnessRegistry` 在两真实家族间按 policy/auto 选择;`PreparedAttempt` 的 `RuntimePlan` 富契约(tool 归一 `normalize_tools`、outcome 分类 `classify_outcome`、silent 判定 `is_silent`)已落实为 **core 纯数据 + trait**,证明其不依赖任何具体 runtime([07 §4](07-plugin-runtime-architecture.md)、[ADR-0008](../adr/0008-agent-runtime-as-pluggable-harness.md))。**不变量守恒**:子进程 spawn 严格隔离在 adapter crate,core 仍零子进程、同编 `wasm32`,端上永远 embedded;core 9 单测 + `adapters-mem` 2 端到端 + cli-harness 5 集成测试(见 [04 #14/#27](04-spike-evidence.md))。
9. **AI 抽象:HTTP LLM/Embedding 适配器**:✅ **已落地** —— `crates/adapters-http-llm`(`HttpLlm`/`HttpEmbedding`)实 `LlmPort`/`EmbeddingPort`,打 **OpenAI/LiteLLM 兼容端点**(`reqwest` json+rustls-tls,`AgentAction` serde tag 与模型 JSON 1:1,`strip_fences` 容错);`apps/server` 经 `select_llm_and_embedding()` 按环境变量(`AGISTACK_LLM_BASE_URL`/`_MODEL`/`_API_KEY`、`AGISTACK_EMBED_MODEL`)在缺省**离线 stub**(`StubLlm`+`HashEmbedding`,零网络零 key)与 HTTP 适配器间切换。**7 测试绿**(裸 `tokio` TcpListener mock-server:extract/decide/embedding/500→error/坏 JSON→error,断言提示与 auth 头);reqwest/tokio 严格隔离在 adapter 与 server、绝不入核心 `wasm32`,根 `cargo test --workspace` 升至 **54 绿**(见 [04 #20](04-spike-evidence.md)、[03 §2](03-platform-adapters.md))。**嵌入半进一步对活模型服务器实测**(#38):`HttpEmbedding` 打运行中的 Ollama OpenAI 兼容 `/v1/embeddings`(`qwen3-embedding:0.6b`,1024 维),断言维度稳定 + 近确定性(`cosine>0.999`,实测 1.0)+ 语义合理性(related 0.72 ≫ unrelated 0.28),env-gated 离线 skip 恒绿——退役 #20 的「仅 mock」尾注,证 cloud↔local 双适配对**真嵌入模型**成立。**chat 半亦对活 coding-plan provider 实测**(#39:`HttpLlm` 打 GLM `glm-4.6` + MiniMax `MiniMax-M2` OpenAI 兼容 `/chat/completions`,新增公共 `complete()` + `strip_reasoning`/`clean_structured`(使 MiniMax-M2 等推理模型 `<think>` 块下结构化抽取仍解析),`complete` 回显 PONG + `extract_memory` 端到端合式 draft 双绿,env/限流门控离线 skip 恒绿)——退役「无 chat 模型」尾注、**F7 非流式 chat 半落地**。**并新增混合检索精排端口**(#40:`RerankPort`+`RerankHit`+`HttpRerank`,Cohere/Jina/BGE 兼容 `/rerank`,对活 `BAAI/bge-reranker-base` 交叉编码器实测——相关文档排 #1、分值远超干扰项;端口同编 `wasm32`)。根 `cargo test --workspace` 由 206 升至 **211 绿**。后续:流式 token(SSE)、WS 事件桥、多 provider 路由/回退、端上本地 LLM + 交叉编码器,按 A 方案标注 future。
10. **local-first 同步层(version-vector + LWW)**:✅ **已落地** —— `crates/core/src/sync.rs` 提供纯核心 `Replica`/`SyncRecord`/`VersionVector`/`reconcile`:两副本离线各自变更(`Clock` 端口盖 `at_ms`、`ChangeLog` 出端口捕获)→ 重连交换 **version-vector delta**(只发对端缺的)→ 按 **LWW-register 总序** `(version, at_ms, replica_id, seq)` 确定性消解冲突 → 双端 `view` 字节级一致。`(replica_id,seq)` 全局唯一保证 `merge` 交换/结合/幂等(CRDT 律),reconcile 顺序无关。core-only、零 tokio/std::time、同编 `wasm32`;core 6 单测 + `adapters-mem/tests/sync.rs` 2 集成(phone/laptop 离线冲突→收敛、安静重连 no-op),根 `cargo test --workspace` 升至 **62 绿**(见 [04 #21](04-spike-evidence.md)、[08 §7](08-control-data-plane-separation.md))。后续:富 CRDT 类型(RGA 序列/PN-计数器)、真实持久化与网络传输适配器、与 #6 Plan DAG 的会话状态同步打通,按 A 方案标注 future。
11. **扩展性收尾:L2 Rhai Skill 引擎**:✅ **已落地** —— `crates/plugin-host/src/skill.rs` 加 L2 Skill 引擎:`Skill`(name/version/description/`trigger` Rhai 表达式/`steps` 工具名列表)= **纯数据**(serde,ship 端上为配置、端上编译)+ **沙箱化 Rhai 触发器**;`SkillEngine` 硬化(`set_max_operations(50_000)` 指令预算[确定性 runaway 防护,**无墙钟/std::time**]+ expr-depth/string/array/map 上限 + `disable_symbol("eval")`,Rhai 默认无 FS/网络);`run` 全程钉一份 `registry.snapshot()`(**轮次边界隔离**)按声明顺序 invoke 已准入 L1 工具。**信任轴守恒**:Skill = 配置层(无原生码、无环境权限),只能编排已按自身信任层(可信 `dyn Trait` / 不可信 WASM)准入的工具;语义路由(哪个 skill 匹配意图)仍归 agent(**Agent First**),Rhai 触发器是作者声明的**确定性配置**。样例 `skills/weather-skill.json` 经 `include_str!` 端到端测试。`rhai` 1.25 `features=["sync"]`,wasm32 经 `getrandom` `wasm_js` 后端(浏览器 Web Crypto);**9 测试绿**,根 `cargo test --workspace` 升至 **71 绿**、core + plugin-host `wasm32` 均绿(见 [04 #23](04-spike-evidence.md)、[02 §6](02-extensibility.md))。后续:MCP-over-WASM 工具、Skill 语义触发与 agent 路由打通、30+ 工具实际绞杀迁移,按 A 方案标注 future。
12. **逐平台构建矩阵(Makefile)**:✅ **已落地** —— [`Makefile`](../../Makefile) 把每平台已验证构建命令收敛为一键 target:`server`(release 5.46 MB)·`wasm-web`(wasm-pack 包 + node 冒烟)·`desktop`(Tauri 全树编译 + 无头测试)·`android`(NDK 交叉编 `.so` + Kotlin)·`ios`(`scripts/build-ios.sh` → XCFramework + 模拟器实跑)·`bench`(评分卡)·`wasm-core`/`ci`(最小门禁)·`all`(全部免 SDK 产物)·`help`(自描述)。**本机实跑** wasm-core/server/wasm-web/desktop/bench 五个免 SDK target 全绿(`make bench` 实读产物体积 → 14/14 ✅ GO),android/ios 镜像 #12/#13 已验证命令。出厂矩阵综述 [09-shipping-matrix](09-shipping-matrix.md)(产物/体积/证据 + 留 future 项),收口 Phase 5 可码部分。后续:`cargo-ndk`/`cargo-component`/CI matrix runner 固化、真机签名分发、`tauri-cli` 完整 bundle、Python 逐能力绞杀替换,按 A 方案标注 future(见 [04 #24](04-spike-evidence.md))。
13. **生产绞杀迁移第一垂直(P1 记忆/情节/召回)**:✅ **已落地** —— 从 Spike/Phase 1 基座迈入 **Phase 5 生产绞杀**:Rust 服务器**读写 Python 同一套 Postgres schema**(零数据迁移),经反向代理网关 [`apps/gateway`](../../apps/gateway) 按路径前缀把已绞杀能力(`/memories`/`/episodes`/`/recall`)路由到 Rust、其余 ~580 端点仍走 Python,同 `/api/v1` 契约 + 同 `ms_sk_` Bearer + 字节兼容 JSON → 前端零改动、可灰度、可回滚。**F1** [`crates/adapters-postgres`](../../crates/adapters-postgres)(sqlx+pgvector,server-only)对 Python `memories`/`api_keys`/`projects` 逐字读写 + `agistack_` 附加表承向量/checkpoint(**绝不 ALTER Python 表**),真 `pgvector:pg16` 容器 4 集成测试绿;**F2** [`apps/server/src/auth.rs`](../../apps/server/src/auth.rs) 认证/多租户中间件(SHA256 与 Python 字节一致、401 路径对齐、`project_id` 收敛);**P1** [`apps/server/src/prod_api.rs`](../../apps/server/src/prod_api.rs) 6 端点响应与 Python **字节不可区分**。端到端(真服务器+真网关+mock Python)curl 全流程通过;根 `cargo test --workspace` 由 71 升至 **87 绿**、core `wasm32` 仍绿(Postgres/网关重依赖严格 server-only、绝不入核心)。综述 [10-production-migration](10-production-migration.md)(P1..P8 全量波次计划),见 [04 #25](04-spike-evidence.md)。后续:P2 身份/租户 → P3 Agent 运行时(吞并 Wave L/M/N 硬化)→ P4 图谱 → P5 沙箱/MCP → … → P8 下线 Python,各为独立后续波次;真正的「龙」(Ray 分布式、LiteLLM 100+ provider、Neo4j 抽取、MCP/Docker 沙箱、流式 WS 桥)按 A 方案逐波次落地,MVP 期经网关与 Python 并存。
14. **生产绞杀迁移第二垂直(P2 登录/身份·租户读写·项目读写·项目成员写·tenant member 写·邀请流/device-code/shares/trust)**:✅ **已落地** —— 延续「共享库之上的绞杀者」:`POST /auth/token`(登录)+ `POST /auth/oauth/{provider}/callback`(501 parity)+ `POST /auth/device/{code,approve,token}` 翻网关到 Rust,`GET /tenants`/`GET /tenants/{id}`、`POST /tenants`、`PUT/DELETE /tenants/{id}`、`POST/PATCH/DELETE /tenants/{id}/members*` 与 `GET/POST /projects`、`GET/PUT/DELETE /projects/{id}`、`GET /projects/{id}/stats`、`GET /projects/{id}/members` 走 method-scoped 翻转,`POST /projects/{id}/members` 与 `PATCH/DELETE /projects/{id}/members/{user_id}` 也走 method-scoped 翻转,`POST/GET/DELETE /tenants/{id}/invitations` 与 `GET /invitations/verify/{token}`/`POST /invitations/accept/{token}` 也走精确翻转,`GET /shared/{share_token}` 走 single-child method rule,`GET/POST /tenants/{id}/trust/policies`、`GET /tenants/{id}/trust/policies/check`、`POST /tenants/{id}/trust/approval-requests`、`POST /tenants/{id}/trust/approval-requests/{record_id}/resolve`、`GET /tenants/{id}/trust/decision-records` 与 `GET /tenants/{id}/trust/decision-records/{record_id}` 走精确 tail method rules;`/memories/{id}/shares*` 由既有 P1 `/memories` 前缀覆盖且本轮已补 Rust 实现。`GET /tenants/{id}/members`、未覆盖的 P5 sandbox runtime siblings、更深 `/shared/*`/`/trust/*` siblings 等仍走 Python。登录**逐字复刻** Python `auth.py`(401 双路径 + `WWW-Authenticate` 条件头 + `is_superuser`→admin 权限 + 铸 `ms_sk_` 登录会话 key 而非 JWT + **扁平无时间戳 `Token` 故精确可复现可翻转**);**摘要 parity 铁证** —— `insert_api_key` 内部 `sha256_hex(plaintext)` 与认证器同一摘要,铸 key 立即可认证(经真 `pgvector:pg16` 容器 `login_and_tenant_reads_roundtrip` 集成测试端到端验证)。`adapters-secrets`(bcrypt/`ms_sk_`/uuid/token/device user-code,server-only)+ `RedisDeviceGrantStore`(server-only Redis TTL grant)+ `PgUserStore`/`PgTenantRepository`/`PgProjectReadRepository`/`PgInvitationRepository`/`PgShareRepository`/`PgTrustRepository` + `identity.rs`/`identity_api.rs`/`shares_api.rs`/`trust_api.rs`(`router_public`/`router_authed` 拆分)。项目读写覆盖 list/get/create/update/delete/stats/members 的 Python wire shape(default memory/graph/sandbox config、backend summary、stats dashboard、member list、owner_ids、403-before-404/422 UUID 顺序、create tenant admin 权限、update owner/admin 权限与 patch explicit-null/store env 归一化);项目成员写覆盖 add/update/delete 的 Python 错误顺序、role 默认/校验、owner 保护、`editor` add/update 权限差异和 204 删除;邀请流覆盖 admin 权限、重复 pending 409、verify invalid/expired 200 invalid、accept 写 `user_tenants`;device-code 覆盖 Redis TTL grant、428 pending、approved token 单次消费;shares 覆盖 create/list/delete/public token、target duplicate、`permissions.view is true` 与 access_count;trust 覆盖 policy list/create/check、approval submit/resolve、decision record list/get、tenant admin/superuser/workspace 归属、非法 decision→404 与 `allow_always` 自动创建 always policy;新增 `project_view`/`project_page`/`project_stats`/`project_members`、`project_member_added`/`project_member_updated`、`tenant_member_added`/`tenant_member_updated`、`invitation_response`/`invitation_list`/`invitation_verify`、`device_code_response`/`device_approve_response`/`device_token_response`、`share_response`/`share_list`/`shared_memory`、`trust_policy_response`/`trust_policy_list`/`trust_check_response`、`decision_record_response`/`decision_record_list` golden。当前根 `cargo test --workspace` **407 绿**;core wasm gate 已在 rustup cargo/rustc(`~/.cargo/bin` 优先)下通过。后续:P3 Agent 运行时与 P6 剩余协作面,逐波次 future。
15. **绞杀 parity 硬门禁(F3,可复用契约框架)**:✅ **已落地** —— 绞杀翻转的**放行门**从 P1 的隐式假设升为**可复用库 + 硬门禁**:[`crates/parity`](../../crates/parity)(`agistack-parity`,**仅依赖 `serde_json`、同编 `wasm32`**)`compare(golden,actual)→ParityReport` 递归比对——对象**键集**顺序无关、数组**顺序+长度**敏感、标量**精确含格式**(`…Z` ≠ `…+00:00`、`1` ≠ `1.0`、UUID 大小写),动态字段用 matcher token(`<uuid>`/`<iso8601>`/`<ms_sk>`/`<int>`/`<string?>`/`<urlsafe_token>`/`<device_user_code>`…,括号内 `?` 兼容 pydantic `Optional`),格式校验器 `is_iso8601_utc_z`(拒 `+00:00`)/`is_canonical_uuid`/`is_ms_sk_key`/`is_error_envelope`(FastAPI `{"detail":…}`),**15 自测绿**。**回溯应用**到 P1/P2/P5/P6 真实 wire 形状:[`apps/server/tests/golden`](../../apps/server/tests/golden) 现有 59 份契约派生 golden(源自 Python schemas 与 P5/P6 contract),覆盖 P1 memory/episode/recall、P2 login/tenant read-write/project read-write/member writes/tenant writes/invitations/device-code/shares/trust wire,以及 P5 project sandbox lifecycle response/health/stats/action/list/execute/proxy-auth-cookie/desktop-terminal/http-service control/path/WS/preview-host HTTP/WS + desktop HTTP/WS(noVNC websockify) + terminal/ttyd WS envelope proxy + ttyd resize wire protocol + Docker image pull policy + durable terminal reconnect/session resume registry + MCP OAuth-like upstream token foundation + local/tunnel sandbox semantics + MCP browser WS proxy/query-cookie auth bridge + Redis-backed HTTP service/preview-session/terminal-session registry foundations + skill store/versioning foundation,以及 P6 workspace/task/topology/blackboard + blackboard-files + plan snapshot + outbox leasing/retry + outbox retry/delivery/node/recover-stale action foundation + outbox worker foundation + delivery loop safety gate + handoff/attempt_retry + worker_launch admission + pipeline_run admission + supervisor retry admission + supervisor missing-attempt recovery + supervisor reported-attempt reconcile + supervisor terminal retry release + supervisor accepted terminal projection + accepted worktree skipped/already-merged integration + supervisor failed worktree integration reopen + supervisor superseded accepted attempt projection + supervisor root-goal progress reconcile + pipeline_run success reflection + pipeline_run running recovery + pipeline_run stale running finish + pipeline_run durable run creation + pipeline stage-run persistence + production-ready gate foundations,并以 gateway e2e 覆盖 P5 path 数据面 proxy/WS 与 preview-host host-based HTTP/WS 精确翻转。**负控**:改坏一份 golden → 门禁以精确路径 `$.processing_status`/`$.updated_at` 报错,证非空转。当前根 `cargo test --workspace` **407 绿**、core `wasm32` 仍绿(parity 库无 server-only 依赖)。自 P2 起 [10-production-migration §F3](10-production-migration.md) 由「P1 最小切片」升为**硬门禁**,见 [04 #30](04-spike-evidence.md)。后续:与 live Python OpenAPI 并跑捕获 golden(当前为契约派生);每个 P3+ 波次翻网关前须过此门禁。
16. **P4 端上知识图谱可移植切片(决策 4)**:✅ **已落地** —— 退役风险 #5 的可完全验证部分:把 Python `src/infrastructure/graph/` 的**图模型 + 混合检索排序数学**提炼为运行时无关的 core 原语,并以**两套适配器**实现同一 `GraphStore` 端口。**core** [`crates/core/src/graph.rs`](../../crates/core/src/graph.rs)(纯数学·零依赖·同编 `wasm32`,`f64::powf` 于 `wasm32` 链接通过):`rrf_fuse`(k=60、vec 0.6+kw 0.4)/`time_decay`(半衰期 30d,`0.5^(age/hl)` 代数等价 Python `exp`)/`mmr_rerank`(λ=0.7)/`jaccard`,**逐字复刻 Python 常量公式**,故端上近似与服务器 Neo4j 在**排序层**一致;`GraphEntity`/`Relationship`/`Subgraph` 模型 + `GraphStore` 端口(全 `project_id` 收敛)。**双适配器** `InMemoryGraphStore`([`adapters-mem`](../../crates/adapters-mem/src/graph.rs),`petgraph` BFS,**同编 `wasm32`** 证浏览器)与 `SqliteGraphStore`([`adapters-device`](../../crates/adapters-device/src/graph.rs),SQLite 关系表 + `petgraph`,端上耐久、重开持久)；**跨层 parity 测**断言二者对同数据在 depth 0..=4 子图 + 邻居 + 搜索 `assert_eq!` 完全一致。core 14 + mem 9 + device 5 + 跨层 1 测试;根 `cargo test --workspace` 由 148 升至 **177 绿**、core + `adapters-mem` `wasm32` 均绿(rusqlite 绝不入 wasm)。见 [04 #31](04-spike-evidence.md)、[01-portable-core §3](01-portable-core.md)。后续:LLM 驱动 reflexion 抽取(需 `LlmPort`)、Louvain 社区、端上 LLM 推理,按 A 方案标注 future。
17. **F8 生产 Neo4j 图谱适配器(同一 `GraphStore` 端口第三适配器)**:✅ **已落地(对活库实测)** —— 在 #16 的内存/SQLite 双适配器之上补齐**生产服务器层**,证「同一端口三套后端(浏览器/端上/服务器)对同数据返回一致结构」。**server-only** [`crates/adapters-neo4j`](../../crates/adapters-neo4j)(`neo4rs 0.8` 异步 Bolt,拖 tokio+TLS 故严格隔离出 core/wasm,ADR-0001):`Neo4jGraphStore` 实 `GraphStore` 六法,对 **Python 同一套 Neo4j** 说 Bolt(`:Entity` 节点 + 默认 `MENTIONS` 边),故绞杀翻 `/graph/*` **零数据迁移**;`upsert_entity` MERGE on `(uuid, project_id)`(镜像内存 `(project,uuid)` 键 → 项目隔离)、关系类型经 `sanitize_rel_type` 校验为标识符 + 反引号包裹防注入、`subgraph` 经 Bolt 取项目切片后跑与其它两层**同一份深度界 BFS** 故子图逐字一致。**跨层 parity 集成测**对**运行中的 Docker Neo4j**(`neo4j:5.26-community`)播种钻石 DAG,断言 `Neo4jGraphStore` == `InMemoryGraphStore` 于 `get_entity`/`neighbors`/`subgraph`(0..=4)/`search` 完全一致 + 跨项目泄漏否控;hermetic(唯一 `project_id` + `DETACH DELETE`)、环境门控(不可达则 `[skip]` → 离线恒绿),**本会话对活库实跑 0.70s 通过(非 skip)**。根 `cargo test --workspace` 由 177 升至 **179 绿**、core `wasm32` 仍绿。见 [04 #32](04-spike-evidence.md)、[10-production-migration §6.3](10-production-migration.md)。后续:Episodic/Community 节点 + 向量/全文索引、LLM reflexion 抽取管线。
18. **F5 生产 Redis Streams 事件总线(新增 `EventStream` 端口 + 内存/Redis 双适配器)**:✅ **已落地(对活库实测)** —— 补齐 P3/P6 的 **agent 事件总线地基**(Rust 侧此前完全缺失事件流)。core 加 `EventStream` 端口([`crates/core/src/ports.rs`](../../crates/core/src/ports.rs):`append(topic,payload,max_len)->id` / `read_after(topic,after_id,limit)`,payload 为不透明序列化事件 JSON → core 与具体事件枚举解耦、**同编 `wasm32`**)+ `StreamEntry` + `CoreError::Event`。**内存/浏览器适配器** `InMemoryEventStream`([`adapters-mem`](../../crates/adapters-mem/src/event_stream.rs),id 零填充 `{:020}` 使字典序==插入序,**同编 `wasm32`**,作 Redis 层 parity 预言机)5 单测;**Redis 生产适配器** `RedisEventStream`([`crates/adapters-redis`](../../crates/adapters-redis),`redis 0.27` server-only 严格隔离出 core/wasm)对 **Python 同一套 Redis Streams**(`agent:events:{conv}`)说 `XADD MAXLEN`(精确无 `~` → 确定性保留最后 N)/`XRANGE`(`(<id>` 独占分页),故绞杀可把事件**生产**从 Python 翻到 Rust、WS 桥继续消费同一 stream **零数据迁移**。**跨层 parity 集成测**对**运行中的 Docker Redis**(`redis:7-alpine`)播种同数据,断言 `RedisEventStream` == `InMemoryEventStream` 于 payload 序列 + 增量分页 + 精确 MAXLEN 裁剪(**ids 不同、payload 与顺序同**);hermetic(唯一 topic + `DEL`)、环境门控(不可达则 `[skip]` → 离线恒绿),**本会话对活库实跑二测 0.02s 通过(非 skip)**。根 `cargo test --workspace` 由 179 升至 **186 绿**、core `wasm32` 仍绿。见 [04 #33](04-spike-evidence.md)、[10-production-migration §3(F5)](10-production-migration.md)。后续:消费组 `XREADGROUP` fan-out、多流(`hitl:responses`/`agent:control`/`workspace:*`)、WS 事件桥(F7)——随 P3/P6。
19. **F6 生产 S3/MinIO 对象存储(新增 `ObjectStore` 端口 + 内存/S3 双适配器)**:✅ **已落地(对活库实测)** —— 补齐 P5/P7 的 **对象存储地基**(Rust 侧此前完全缺失 blob 存储)。core 加 `ObjectStore` 端口([`crates/core/src/ports.rs`](../../crates/core/src/ports.rs):`put(key,bytes,content_type)` / `get`→`Option<Vec<u8>>` / `stat`→`Option<ObjectMeta>` / `delete`(幂等) / `list(prefix)`(升序),值为不透明字节 → core 与具体 artifact schema 解耦、**同编 `wasm32`**)+ `ObjectMeta{size,content_type}`(复用 `CoreError::Storage`);缺 key → `None`(镜像 Python 404),无类型两层归一 S3 默认 `application/octet-stream`。**内存/浏览器适配器** `InMemoryObjectStore`([`adapters-mem`](../../crates/adapters-mem/src/object_store.rs),`BTreeMap` 天然升序 → `list` 与 S3 字节对齐,**同编 `wasm32`**,作 S3 层 parity 预言机)6 单测;**S3 生产适配器** `S3ObjectStore`([`crates/adapters-s3`](../../crates/adapters-s3),`aws-sdk-s3 1` server-only 严格隔离出 core/wasm)对 **Python 同一套 S3/MinIO 桶**(artifacts/attachments)说 `PutObject`/`GetObject`/`HeadObject`/`ListObjectsV2`(`force_path_style` + 缺桶自建),故绞杀可把 blob 读写从 Python 翻到 Rust、Python 侧继续读写同一桶 **零数据迁移**。**跨层 parity 集成测**对**运行中的 Docker MinIO**(`minio/minio:latest`)播种同数据,断言 `S3ObjectStore` == `InMemoryObjectStore` 于 body(含二进制)+ stat(size+content_type)+ 前缀 list(排序)+ 删后取 `None`;hermetic(唯一桶 + 删净对象)、环境门控(不可达则 `[skip]` → 离线恒绿),**本会话对活库实跑 0.30s 通过(非 skip)**。根 `cargo test --workspace` 由 186 升至 **193 绿**、core `wasm32` 仍绿。见 [04 #34](04-spike-evidence.md)、[10-production-migration §3(F6)](10-production-migration.md)。后续:分片/流式上传、`refresh-url` 预签名——随 P5/P7。
20. **F9 生产 MCP-over-WebSocket `ToolHost` 客户端(既有 `ToolHost` 端口第三后端)**:✅ **已落地(对活服务器实测)** —— 补齐 P5 沙箱波的**远程工具传输地基**。**与 F5/F6/F8「存储后端字节 parity」本质不同**:MCP 是**协议客户端**而非存储后端,故证据是「**活协议一致性 + 真工具往返**」而非字节 parity。**关键洞察**:既有 `ToolHost` 端口([`crates/core/src/ports.rs`](../../crates/core/src/ports.rs))文档原文即写「执行工具——既含可信内置也含沙箱化第三方/MCP 能力」,故 MCP 客户端实现**既有端口零改动**,证明信任/传输轴(ADR-0002)当初分解正确——它是 `ToolHost` 的**第三后端**:原生注册中心(`plugin-host`)/ WASM 沙箱(`adapters-wasmtime`)/ **远程 MCP(`adapters-mcp`)**。**server-only** [`crates/adapters-mcp`](../../crates/adapters-mcp)(`tokio-tungstenite 0.24` WS,拖 tokio 故严格隔离出 core/wasm,ADR-0001):`WsMcpToolHost` 实 `ToolHost`——`connect(url)` 异步握手(`initialize`→`protocolVersion 2024-11-05`→`notifications/initialized`→`tools/list` **缓存工具名**,因服务器声明 `listChanged:false`)、`list_tools()` 返缓存名(同步契合端口)、`call(tool,input)` 发 `tools/call` 按 id 关联应答,JSON-RPC `error` 与工具级 `isError:true` 双归 `CoreError::Tool`。**活协议一致性 + 往返集成测**对**运行中的 Docker `sandbox-mcp-server:0.1.0`**(:18765)断言握手 `server_name` 非空、`tools/list` 含 read/write/bash/list(52 工具)、`&dyn ToolHost` 可用、**hermetic write→read 精确字节往返**、未知工具/坏 JSON→`Err(Tool)`;工作区根 `/workspace`(越界写被拒);环境门控(`MCP_TEST_URI` 不可达则 `[skip]` → 离线恒绿),**本会话对活服务器实跑 0.02s 通过(非 skip)**。根 `cargo test --workspace` 由 193 升至 **197 绿**(+4:`extract_text` 3 单测 + 活库协议一致性 1 集成)、core `wasm32` 仍绿(tokio-tungstenite 绝不入 core)。见 [04 #35](04-spike-evidence.md)、[10-production-migration §3(F9)](10-production-migration.md)、[ADR-0002](../adr/0002-untrusted-plugins-wasm-only.md)。后续:Docker 容器生命周期(`bollard`)、desktop/terminal/noVNC 代理、OAuth-like token——随 P5。
21. **F9 生产 Docker 容器生命周期(新增 `ContainerRuntime` 端口 + 内存/bollard 双适配器)**:✅ **已落地(对活 Docker daemon 实测)** —— 补齐 #20 之下的**沙箱制备层**(Python `MCPSandboxAdapter` 容器生命周期),是 F9 的另一半。**与 #20 同属 lifecycle 面**故证据是「**状态机一致性**」(Created→Running→Exited→absent)而非字节 parity。core 加 `ContainerRuntime` 端口([`crates/core/src/ports.rs`](../../crates/core/src/ports.rs):`create`/`start`/`status`/`stop`/`remove`/`list`,+ `ContainerSpec`/`ContainerState`/`ContainerStatus`/`CoreError::Container`,**运行时无关·同编 `wasm32`**)。**内存/测试适配器** `InMemoryContainerRuntime`([`adapters-mem`](../../crates/adapters-mem/src/container_runtime.rs),`BTreeMap` 建模状态机,**同编 `wasm32`**,作 Docker 层状态机预言机)4 单测;**bollard 生产适配器** `DockerContainerRuntime`([`crates/adapters-docker`](../../crates/adapters-docker),`bollard 0.17` server-only 严格隔离出 core/wasm)走本地 socket create/start/status/stop/remove + 托管标签 `agistack.managed=true` 使 `list` 只返本层容器,状态枚举 CREATED/RUNNING/EXITED → `ContainerState`、404/304 → no-op。**跨层状态机一致性集成测**对**运行中的 Docker daemon**(socket 活跃 + `redis:7-alpine` 已在本地,**无需 pull**)真起真停一个容器,断言状态序列 `[Created,Running,Exited]` + `list(label)` 恰返该容器 + 删后 absent,**再对 `InMemoryContainerRuntime` 跑同一 `walk_lifecycle` 助手 `assert_eq!` 序列完全一致**;hermetic(唯一 per-run 标签 + `force` 删净,实测无残留)、环境门控(socket 不可达则 `connect()` 失败 → `[skip]` → 离线恒绿),**本会话对活 daemon 实跑 1.03s 通过(非 skip)**。根 `cargo test --workspace` 由 197 升至 **202 绿**(+5:内存 4 单测 + 活 daemon 状态机一致性 1 集成)、core `wasm32` 仍绿(bollard 绝不入 core)。见 [04 #36](04-spike-evidence.md)、[10-production-migration §3(F9)](10-production-migration.md)。后续:server project sandbox lifecycle/durable registry/tenant list/execute/per-sandbox MCP resolver foundation 已在 [04 #58](04-spike-evidence.md)/[#59](04-spike-evidence.md)/[#60](04-spike-evidence.md)/[#61](04-spike-evidence.md)/[#62](04-spike-evidence.md)/[#63](04-spike-evidence.md) 落地;镜像 pull 已在 #80 落地;隧道/gateway 语义、desktop/terminal/noVNC WS 代理仍随 P5。
22. **F10 生产 SMTP 事务邮件(新增 `EmailSender` 端口 + 内存/lettre 双适配器)**:✅ **已落地(对活 mailpit 实测)** —— 补齐 P2 邀请邮件 + P7-G4 通知的**事务邮件出口地基**(Rust 侧此前完全缺失邮件出口)。**与 F5/F6/F8 存储字节 parity、F9 协议/状态机一致性本质不同**:`EmailSender` 是**纯 I/O 副作用出口**(不是值存储、也不是可反查状态机),故证据是「**行为一致性**」——内存 fake 精确记录一次真实 SMTP 发送所传的信封,mailpit 收件箱确认真到达,两者信封逐字对齐,**非**字节 parity。core 加 `EmailSender` 端口([`crates/core/src/ports.rs`](../../crates/core/src/ports.rs):`async fn send(&EmailMessage)`,+ `EmailMessage{from,to,subject,body_text,body_html}`(derive `Eq` 供 `assert_eq!` 信封预言)、`CoreError::Email`,**运行时无关·同编 `wasm32`**——纯 `String` 字段 + `async_trait`,lettre 绝不入端口签名)。**内存/端上适配器** `InMemoryEmailSender`([`adapters-mem`](../../crates/adapters-mem/src/email.rs),`Mutex<Vec<EmailMessage>>` 记录信封,**行为预言机** + **同编 `wasm32`**;adapters-mem 无 tokio 故异步测走 `futures::executor::block_on`)2 单测;**lettre 生产适配器** `SmtpEmailSender`([`crates/adapters-smtp`](../../crates/adapters-smtp),`lettre 0.11` server-only 严格隔离出 core/wasm)`plaintext`(本地/mailpit)/`relay`(生产 TLS 提交)/`with_default_from`,有 HTML → `alternative_plain_html` 多部分、否则 `TEXT_PLAIN`,`"Name <addr>"`/裸 addr → `Mailbox`。**跨层行为一致性集成测**对**运行中的 mailpit**(`public.ecr.aws/supabase/mailpit:v1.22.3`,:1025 SMTP/:8025 API,已在本地)清空邮筒 → 经 `SmtpEmailSender` **真发**一封 unique-nanos-subject 多部分信 → 轮询 HTTP API 直到到达 → 断言收件箱 `Subject`/`To`/`From` + 全文 `Text`/`HTML` 两部分 → **再对 `InMemoryEmailSender` 发同一 `EmailMessage` 断言信封一致** → 删净;hermetic(unique subject + 末尾清空)、环境门控(`AGISTACK_MAILPIT_URL`/`AGISTACK_SMTP_PORT` 不可达则 `[skip]` → 离线恒绿),**本会话对活 mailpit 实跑 0.06s 通过(非 skip,真发真收一封信)**。根 `cargo test --workspace` 由 202 升至 **204 绿**(离线 +2 内存单测)、有活 mailpit 时 **205**(+1 活库行为一致性集成)、core + `adapters-mem` `wasm32` 仍绿(lettre 绝不入 core/mem)。见 [04 #37](04-spike-evidence.md)、[10-production-migration §3(F10)](10-production-migration.md)。后续:生产 TLS 提交路径(submission :587/:465)、模板渲染、Feishu/webhook 通道、退信处理——随 P7-G4。
23. **AI 抽象 chat 半对活 coding-plan provider 实测(GLM + MiniMax)**:✅ **已落地(对活 provider 实测)** —— #20/#9 的 chat 半此前仅 mock、#38 已升 embedding 半为活 Ollama;本条把 **chat 半升为对两家真实托管 coding-plan 模型实测**:`HttpLlm` 打 **GLM**(z.ai coding plan `glm-4.6`)与 **MiniMax**(`MiniMax-M2`)OpenAI 兼容 `/chat/completions`。**关键适配器改进(真 provider 暴露的真缺口)**:`MiniMax-M2` 是推理模型、答案前置 `<think>…</think>`,原 `strip_fences` 不剥故结构化解析会失败——新增 `strip_reasoning`+`clean_structured`(先剥推理块再剥围栏,**3 新单测**),及公共 `complete()`(裸单轮补全)。**活库集成测** [`tests/llm_live.rs`](../../crates/adapters-http-llm/tests/llm_live.rs):每 provider `complete("…PONG")` 回显 + `extract_memory` 端到端合式 `MemoryDraft`;env 门控(无 key/不可达/**限流 429** → `[skip]` → 离线 `cargo test --workspace` 恒绿),**本会话对两家实跑通过**(MiniMax tags=6 entities=6、GLM tags=6 entities=5)。根 `cargo test --workspace` 由 206 升至(含 #24)**211 绿**、core `wasm32` 仍绿(reqwest/tokio 绝不入核心)。见 [04 #39](04-spike-evidence.md);**退役上一会话「本环境无 chat 模型」尾注、F7 非流式 chat 半落地**([10 §3 F7](10-production-migration.md))。后续:流式 token(SSE `stream:true`)、WS 事件桥、多 provider 路由/回退——随 P3/F7。
24. **新增 `RerankPort` + `HttpRerank`(混合检索精排第二阶段,对活交叉编码器实测)**:✅ **已落地(对活 reranker 实测)** —— Python `hybrid_search` 的重排阶段此前 Rust 侧完全缺失;按用户要求 + 活 infra 补齐。core 加 `RerankPort`(`rerank(query,docs)->Vec<RerankHit>`,纯类型 + async_trait,**同编 `wasm32`**)+ `RerankHit{index,score}` + `CoreError::Rerank`;**server-only** `HttpRerank`([`adapters-http-llm`](../../crates/adapters-http-llm),Cohere/Jina/BGE 兼容 `POST /rerank`,`results[]{index,relevance_score}` 降序,reqwest 隔离 core/wasm)。交叉编码器**联合**打分故序锐、分值为原始 logits(常负)故**一次响应内**比较(相关 ≫ 无关)。**活库集成测** [`tests/rerank_live.rs`](../../crates/adapters-http-llm/tests/rerank_live.rs):query +[相关 async-Rust 文档、香蕉、埃菲尔铁塔] → 断每文档一 hit + 降序 + **相关文档(index 0)排 #1 且 `top>worst+1.0`**;env 门控 `AGISTACK_RERANK_URL`(默认 `http://localhost:8081`)不可达 → `[skip]`(离线恒绿),**本会话对活 `BAAI/bge-reranker-base` 实跑 0.79s 通过**(`top_index=0 top=1.18 worst=-10.19`)。**补齐 P4 混合检索三段**:`EmbeddingPort`(召回)→ `VectorIndexPort`(ANN)→ **`RerankPort`(精排)**。见 [04 #40](04-spike-evidence.md)。后续:端上 Candle 交叉编码器、`top_n` 截断、与 RRF/MMR 融合——随 P4。
25. **P3 事件模型入核(把 Python 165 类型事件分类法 + `EventEnvelope` 线格式移植为纯核心模块)**:✅ **已落地(线格式 parity + 单测)** —— P3(Agent 运行时)是最难波,其 `SessionProcessor` 事件管线依赖稳定的事件类型 + 信封线格式;本条把该分类法从 Python `src/domain/events/{types,envelope}.py` **逐字**移植入 core 作 P3 地基前置。**新纯核心模块** [`crates/core/src/agent/events.rs`](../../crates/core/src/agent/events.rs)(经 `agent/mod.rs`+`lib.rs` 重导出,仅依赖既有 `serde`/`serde_json`+`crate::util::fnv1a`,**零新依赖·同编 `wasm32`**):`EventCategory`(agent/hitl/sandbox/system/message)+ `AgentEventType`(**165 变体**每个 `#[serde(rename)]` 对齐 Python 值逐字)+ 分类实现 `from_wire`/`as_str`/`category()`(**35 显式非-Agent 臂 + `_ => Agent` 默认**,精确复刻 `EVENT_CATEGORIES.get(t, AGENT)`)/`is_delta`(6)/`is_terminal`(3)/`requires_human_response`(6)/`is_internal`(2);`EventEnvelope`(字段/默认/`to_value` 键对齐 `envelope.py::to_dict`,缺省 correlation/causation 序列化为 JSON null 保形)+ `wrap`/`child`/`with_*`/`to_json`/`from_json`;`derive_event_id` FNV-1a→`evt_`+12hex(复刻 Python `evt_{uuid.hex[:12]}` 形状但确定性、零 uuid/rand,`event_id`/`timestamp` 由调用方注入)。**与存储字节 parity 不同**——这是**线格式 parity**:Rust 生产者与既有前端/`EventConverter` 逐字节兼容。**7 单测**覆盖全 165 变体线值往返 + serde rename + category/谓词 parity + 信封 wrap/child/json 往返 + `derive_event_id` 形状。`cargo test -p agistack-core` **43→50**、`cargo test --workspace` **211→218**、core `wasm32` 绿。见 [04 #41](04-spike-evidence.md)、[10 §3 P3](10-production-migration.md)。后续:`SessionProcessor` 消费环(`p3-processor`)、Redis 流投递(F5)、WS 事件桥(F7)——随 P3。
26. **P4 社区检测入核(把 Python `LouvainDetector` networkx 回退路径的社区划分移植为纯核心 Louvain 数学)**:✅ **已落地(确定性 Louvain + 质量/结构 parity + 单测)** —— Python 社区检测的可移植半是 `networkx.louvain_communities`(Neo4j GDS 是服务器专用另一半);本条把该数学逐字移入 core 作服务器 Neo4j 与端上 SQLite+petgraph 图适配器**共用**的社区划分。**新纯核心模块** [`crates/core/src/community.rs`](../../crates/core/src/community.rs)(经 `lib.rs` 重导出,仅 `serde`+`BTreeMap`,**零新依赖·同编 `wasm32`**,无 I/O/`std::time`/rng):`CommunityEdge`(无向加权,`coalesce(weight,1.0)`,并行边求和)+ `Community{name,members,member_count}`;`detect_communities(nodes,edges,min_community_size)`——两相 Louvain(局部移动 `ΔQ = k_i,in − Σ_tot·k_i/2m` + 聚合递归),`min_community_size` 过滤(默认 2)、`Community_{i}` 命名;`modularity(...)` 标准 `Q` 评分。**与 #24/排序数学的字节 parity 本质不同**:networkx Louvain 随机化(无固定 seed)故 exact 社区 id 不可跨语言复现——这里 parity 是**结构+质量**:真划分、局部最优(无单点移动提升 `Q`)、压 all-singletons 与 all-in-one、对固定输入确定。**10 单测**覆盖双三角分裂/模块度压两基线/穷举局部最优/min-size 过滤/确定性/权重敏感/边界(<2 节点·无边·边端点索引·并行边)。`cargo test -p agistack-core` **50→60**、`cargo test --workspace` **218→228**、core `wasm32` 绿。见 [04 #42](04-spike-evidence.md)、[10 §6.3 P4](10-production-migration.md)。后续:端上 SQLite+petgraph 图物化、Community 摘要(LLM)、Neo4j GDS 服务器路径——随 P4。

## 5. 退出准则

- 全部高风险项有明确结论(通过/变通/阻断)。✅ §2 表 #1–#12 各项均有结论:或 ✅ 证伪通过 / 部分证伪(#5 端上图依赖存储+遍历+排序数学 + 生产 Neo4j 驱动均已落地,仅 LLM 抽取/社区/Episodic 节点留 future),或 future(A 方案)显式标注(#6 端上 LLM 推理),无 🎯/⏳ 未决残留。
- 指标对照 §3 阈值,产出 go/no-go 建议。✅ 评分卡 `apps/bench` **已出「GO」**(14/14 ✅,见 [04 #22](04-spike-evidence.md))。
- 若 no-go → 退回 Kotlin Multiplatform 同款 Spike,复用本切片与指标表对照。**(当前为 GO,不触发回退。)**

## 6. 成本现实

~86K LOC 纯核心 + ~319K 基础设施重做,多人季度级工程。**必须绞杀者式增量迁移**(§1),迁移期新旧后端并行,任何阶段都保持可上线。
