# Container-local settings loaded by src.configuration.config.
# Keep the database host on the Compose network; the password is supplied by
# each service environment and expanded by python-dotenv at runtime.
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD:-password}@postgres:5432/memstack
