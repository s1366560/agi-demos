name: E2E Browser Gates

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  web-smoke:
    name: Web Playwright Smoke
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.24.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run backend-independent smoke tests
        env:
          CI: true
        run: pnpm exec playwright test e2e/smoke.spec.ts

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-smoke-report
          path: web/playwright-report
          if-no-files-found: ignore

  # Full backend-dependent E2E remains opt-in until the CI fixture provisions
  # migrations, default users, Ray workers, and LLM credentials. The smoke job
  # above is an always-on browser gate and does not require those services.
  # e2e:
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #
  #     - name: Build API image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: ./Dockerfile
  #         push: false
  #         tags: memstack-api:ci
  #         load: true
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
  #
  #     - name: Start stack
  #       run: |
  #         docker compose up -d neo4j postgres redis
  #         # Wait a bit for DBs to initialize
  #         sleep 10
  #         docker compose up -d api
  #         # Wait for API health
  #         for i in {1..40}; do
  #           curl -sf http://localhost:8000/health && break
  #           sleep 3
  #         done
  #         curl -sf http://localhost:8000/health
  #
  #     - name: Set up Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #
  #     - name: Install pnpm
  #       run: npm i -g pnpm
  #
  #     - name: Install web deps
  #       working-directory: web
  #       run: pnpm install
  #
  #     - name: Install Playwright browsers
  #       working-directory: web
  #       run: pnpm exec playwright install --with-deps
  #
  #     - name: Run E2E tests
  #       working-directory: web
  #       env:
  #         CI: true
  #         VITE_API_URL: http://localhost:8000
  #       run: pnpm run test:e2e
  #
  #     - name: Upload Playwright report
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: playwright-report
  #         path: web/playwright-report
  #         if-no-files-found: ignore
